# Employer Journey

## Verified Journey

1. Employer registration through `/register`.
2. Company creation through `/employer/company`.
3. Pending approval screen through `/pending`.
4. Subscription check through `/employer/billing`.
5. Create job through `/employer/jobs`.
6. Publish job through `/employer/jobs`.
7. Receive candidates through `/employer/pipeline`.
8. Shortlist candidates through the application status API.
9. Schedule interviews through `/employer/interviews`.
10. Send offers through application offer API.
11. Hire candidates through application status API.

## API Coverage

- `authApi.register`
- `companyApi.register`
- `jobsApi.create`
- `jobsApi.setStatus`
- `applicationsApi.updateStatus`
- `applicationsApi.createInterview`
- `applicationsApi.createOffer`

## Completion

Employer journey contract score: complete.

