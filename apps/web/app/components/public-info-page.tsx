import Link from "next/link";
import type { ReactNode } from "react";

export function PublicInfoPage({ eyebrow="Jobs View",title,description,sections,action }: { eyebrow?:string;title:string;description:string;sections:{title:string;body:ReactNode}[];action?:{label:string;href:string} }) { return <main className="mx-auto max-w-4xl px-4 py-12 sm:px-6"><p className="text-sm font-bold text-[var(--cos-primary)]">{eyebrow}</p><h1 className="mt-2 text-3xl font-extrabold sm:text-4xl">{title}</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-[var(--cos-on-surface-variant)]">{description}</p><div className="mt-10 grid gap-5">{sections.map((section)=><section key={section.title} className="rounded-md border border-[var(--cos-outline-variant)] p-5"><h2 className="text-xl font-bold">{section.title}</h2><div className="mt-3 leading-7 text-[var(--cos-on-surface-variant)]">{section.body}</div></section>)}</div>{action?<Link href={action.href} className="mt-8 inline-flex rounded-md bg-[var(--cos-primary)] px-5 py-3 font-bold text-white">{action.label}</Link>:null}</main> }
