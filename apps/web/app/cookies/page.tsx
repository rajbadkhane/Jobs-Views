import { buildMetadata } from "@career-os/shared";import { PublicInfoPage } from "../components/public-info-page";
export const metadata=buildMetadata("Cookie Policy | Jobs View","How Jobs View uses required and optional browser storage.","/cookies");
export default function Page(){return <PublicInfoPage eyebrow="Legal" title="Cookie policy" description="Jobs View uses limited browser storage to keep sessions secure and remember deliberate preferences." sections={[{title:"Required storage",body:"Authentication, CSRF protection, audience choice, and security controls."},{title:"Preferences",body:"Theme, recent searches, saved local filters, view density, and unfinished resume recovery."},{title:"Analytics",body:"Production analytics are enabled only when configured and should follow the applicable consent policy."}]}/>} 
