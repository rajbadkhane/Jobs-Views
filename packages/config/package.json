{
  "name": "@career-os/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configurations for Jobs View monorepos",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  }
}
